[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "invoker"
version = "0.2.0"
dependencies = ["Click"]

[project.scripts]
invoker = "invoker:cli"

[tool.setuptools]
py-modules = ["invoker", "project", "util", "resources"]
